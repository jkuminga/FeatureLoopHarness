# 블로그
---

## 개요
- 지난 포스팅에서는 하네스 엔지니어링의 기본적인 개념을 배우고, OpenAI의 글을 살펴보면서 
하네스 엔지니어링을 위해 필요한 요소들을 알아봄
- 이제, 해당 내용들을 참조하여 진행할 프로젝트에 하네스 구조를 잡아보고 프로젝트를 진행해보자.
- 또한 하네스가 없는 순정 codex를 이용하여 프로젝트를 같이 진행하여, 하네스가 있고 없는 것의 차이점을 한번 보고자 한다.

## 진행 전 고려사항
- omx 사용 시 가장 아쉽다고 생각했던 부분은 no feature list and ralph
 -> prd를 보고 프로젝트 전체 범위에 대한 작업을 그대로 수행해버림
 -> 어떻게 진행되고 있는지 알기가 어려움
- 그래서 수동적이긴 하지만, omx와는 다르게 구현 이전에 기능 리스트를 만들고, 구현 단위를 기능별로 진행하고 싶었다.

- 기능 리스트 관리 및 기능별 구현을 위한 구조는 잘 잡을 수 있었으나, 이전의 mvp 설정 + 설계  + db 생성  + 기능리스트 생성까지의 구조는 결국 사용자가 직접 어느정도 진행한 후 해야하는 상태였다.
- 이런 워크플로우를 omx처럼 강조하면 좋겠다는 생각을 했다. 그래서 이런 저런 방법을 찾아봤다. 
- 하네스 문서들에서 강조하는 구현 피드백 루프 이전의 작업들에 대한 강제성 있는 구성도 해보고자 했다.

## 진행
### 0. 워크플로우 구성하기 
- 구현 이외의 모든 프로젝트 진행 과정에서 필요한 작업들을 7단계로 나누어 관리하도록 함
- Codex 의 Hooks 을 이용하여 매 사용자 요청 시 사용자 요청의 의도를 분석하여
  현재 프로젝트 진행 상태에 맞는 요청인지를 확인한다.
- 이런 것을 넣은 것은 실제 구현 이외의 작업에 대해서는 구현에 사용되는 하네스 구조를 적용하면 안되기 때문에 적용하게 되었다.(설계 등의 요청에 대해서 워크트리 생성 등을 하면 안되기 때문)

#### 훅 작동 과정

#### 의도 분석
- 사용자 프롬프트를 입력받아 핵심 키워드와 alias를 통해 해당 요청의 의도를 분석한다.
- 해당 사용자 프롬프트의 의도(request_type) 와 확신하는 정도(Confidence)를 반환한다.

- confidence는 다음과 같다.
  1. 의도를 확신할 수 있는 핵심 키워드를 한 개만 가지고 있는 경우 -> high
  2. 명확하진 않지만 어느 정도 판단할 수 있는 alias를 1개만 가지고 있는 경우 -> mid
  3. 여러 개의 키워드 및 alias를 가지고 있는 경우 -> low
  4. 어떠한 키워드에도 포함되지 않는 경우 : unknown

- 각 confidence에 따른 진행
  1. high의 경우 그대로 진행
  2. mid의 경우 진행 + additional context로 "해당 요청을 이 intent로 해석했다" 추가
  3. low의 경우 : block
  4. unknown : UNKNOWN 로직 수행(additional prompt)


### 1. AGENTS.md 구성하기
- 가장 먼저 AGENTS.md를 구성한다.
- 해당 파일을 구성하는데 가장 중요한 것은 크기를 최소화 하는 것이다.
- 프로젝트에 대한 최소화된 간단한 정보 + 필수 지침 + 반드시 지켜야 할 개별 작업 파이프라인
으로만 구성하였다.
- OpenAI 글에서는 해당 파일을 프로젝트 관련 문서의 인덱스로 사용할 것을 권장하였으나, 길이가 길어지는 것을 방지하기 위해서 문서 맵 파일을 따로 생성하였고, AGENTS.md에는 필수 지침으로 docs/docs-map.md를 참조하라고 지정해두었다.

#### 작업 파이프라인 설명



### 2. docs 디렉토리 생성
- 이제 프로젝트 관련 문서를 작성할 docs 디렉토리를 생성한다.
- 해당 디렉토리 구성은 아래와 같다.
  (보여주기)

#### docs/features : 기능별로 작업하기 위한 기능별 문서 저장 디렉토리
- 코드 에이전트로 작업을 할 때, MVP에 포함될 기능 단위로 작업을 하는 경우가 많다.
- 그렇기에 구현할 개별 기능들을 모두 해당 디렉토리에 저장하여 진행한다.
- 개별 기능에 대한 디렉토리가 생성되며, 해당 디렉토리 명은 feat-00x-기능명 으로 설정된다.
- 개별 기능에 대한 파일은 다음과 같은 것들로 구성된다.
   - SPEC.md : 기능 구현을 위한 파일 (목표, 범위, 제외할 기능, 흐름, 요구사항, 완료 조건, 제약조건 등을 기술)
   - CHECKLIST.md : SPEC.md를 기반으로 실제 구현을 위한 체크리스트를 담고 있는 파일
   - TEST_CASES.md : E2E 테스트 파일 생성 시 참고할 테스트 케이스 목록을 담고 있는 파일

- 모든 기능들에 대한 목록 및 진행상황은 docs/features/feature-index.md에서 관리한다.
  -> 해당 문서에 존재하지않는, 즉 MVP 외 새로운 기능에 대한 요청이 있는 경우 해당 인덱스 파일에 추가한 후 설계를 진행한다.

- index 단일 파일로만 관리하는 경우 문제가 발생할 수 있기 때문에, features/ 내부에 ready/active/completed/blocked 4개의 디렉토리를 구성하고, 구현 상태에 따라 기능별 디렉토리를 움직이는 식으로 상태를 제어한다.



### 3. .husky : 커밋 훅을 통한 에이전트의 실수 제어
(해당 훅 구조는 "코딩알려주는누나" 유튜부의 하네스 엔지니어링 강의를 참고하였습니다 )
(출처 : https://www.youtube.com/watch?v=3yyLg1xbQSs&t=1023s)
- 에이전트가 실수하는 상황을 최소화 하기 위해서, 훅을 통해 꼭 지켜야 하는 작업들에 대한 강력한 제약을 부여하자.
- husky와 lint-staged를 이용하자.
  - 해당 프로젝트에서는 Pre-commit만 사용했다.
  - 정크 파일 삭제 및 브랜치 정리 등 좀 더 복잡한 작업은 에이전트에게 맡기기로 한다.
  - Pre-commit 파이프라인 
    1. main/master 브랜치에서 src/ 수정 금지 (워크트리에서 진행하기 위함)
    2. feat/fix 브랜치에서 features 내부 설계 문서 존재 확인 (설계 없는 작업 x)
    3. src/ 파일 수정된 경우 강제 단위 테스트 수행
    4. lint-staged 실행





## 결론
- 메인 스트림의 모델들의 단순 구현을 위한 성능은 이미 충분함
- 이제 실수를 줄이고, 모델의 최대한의 성능을 끌어내고 ,장기적인 작업에도 훌륭한 수준으로 진행하기 위한 하네스 구조가 필수적임

- 이러한 하네스 구조를 통해 모델의 성능을 자체 성능보다 더 올릴 수 있다면, 그리고 이러한 방식으로 모델의 동작을 우리가 스스로 제어할 수 있다면
- kimi, ollema 와 같은 비교적 성능이 약간 떨어지지만 가격이 훨신 더 저렴한 모델을 이용하여 사용료의 문제점들을 해결할 수 있을 것이다.